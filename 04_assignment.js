function squareOfWordLength() {
    var JavaScript="JavaScript";
    var GoogleChrome="Google Chrome";
    var DeveloperSmart="Developer Smart";
    console.log(`length of the "JavaScript"word =: ${JavaScript.length} and its square is =:  ${JavaScript.length *JavaScript.length} `);
    console.log(`length of the "GoogleChrome"word =: ${GoogleChrome.length} and its square is =:  ${GoogleChrome.length *JavaScript.length} `);
    console.log(`length of the "DeveloperSmart"word =: ${DeveloperSmart.length} and its square is =:  ${DeveloperSmart.length *JavaScript.length} `);
 
}
squareOfWordLength();
console.log("========================================================================");
var name=function () {
    var givenString ="I am Anguler Developer";
    console.log(`2.1> given string length is: ${givenString.length} AND Total words in string is  4.
     And divided by total number of word:  ${givenString.length/4} `);
console.log(`2.2> multiply by total number of words:  ${givenString.length*4} `);
}
name();